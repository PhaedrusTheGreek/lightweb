# Redis ACL: per-user key isolation
user alice on >alice_dev_password ~alice:* &alice:* +@all
user bob on >bob_dev_password ~bob:* &bob:* +@all

# Default user has full access (used by server processes)
user default on nopass ~* &* +@all
